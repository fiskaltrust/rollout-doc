#### SCU parameters

Each parameter can be specified globally or per SCU (in parentheses).

| Parameter                                                  | Description |
| ---------------------------------------------------------- | ----------- |
| `Mode` (`scu{0-n}_mode`)                                   |             |
| `ModeConfigurationJson` (`scu{0-n}_modeConfigurationJson`) |             |

:::caution

Global values will override SCU specific ones.

:::